info:
  name: List Redirect
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/{{short_code}}"
  auth: inherit

runtime:
  variables:
    - name: short_code
      value: d6xd6RR

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
