info:
  name: List Info Url
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/urls/{{short_code}}"
  auth: inherit

runtime:
  variables:
    - name: short_code
      value: d6xd6RR

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
